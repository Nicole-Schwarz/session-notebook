##Git-Branches and Pull-Requests

# Create Branch

git branch feat/header git switch -c feat/header
git switch feat/header or

work on it...

git push or if new branch -> git push -u origin feat/header

pull request in github

git pull -> feature in main branch
